{
  "name": "core",
  "version": "0.0.0",
  "description": "core package",
  "homepage": "",
  "license": "ISC",
  "module": "dist/core.es.js",
  "main": "dist/core.js",
  "directories": {
    "dist": "dist",
    "test": "__tests__"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "npx ../../node_modules/.bin/jest --config ../../jest.config.js",
    "build": "npx ../../node_modules/.bin/rollup -c ../../rollup.config.js",
    "postbuild": "npm run docs",
    "docs": "npx ../../node_modules/.bin/typedoc src/index.ts --options ../../typedoc.json --tsconfig ../../tsconfig.json"
  },
  "dependencies": {
  }
}
